<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FinSight" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.FinSight.Splash" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/sage</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/finsight_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/terracotta</item>
        <item name="postSplashScreenTheme">@style/Theme.FinSight</item>
    </style>

    <style name="Theme.FinSight" parent="Base.Theme.FinSight" />
</resources>